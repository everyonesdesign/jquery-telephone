;
(function ($) {

    $.fn.telephone = function (options) {

        var $this = $(this);

        //setting options
        var defaults = {
            responsive: true
        };

        options = $.extend(defaults, options);


    };


}(jQuery));

