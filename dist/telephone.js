;
(function ($) {

    $.fn.telephone = function (options) {

        var $this = $(this);

        //setting options
        var defaults = {
            //mask
            //type (discus/electro)
            //speed of discus return, ms
        };

        options = $.extend(defaults, options);

    };


}(jQuery));

